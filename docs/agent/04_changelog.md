# Дневник на промените (Changelog)

Всички забележителни промени в проекта "The Best Idea Eatery" ще бъдат документирани в този файл.
Файловият формат е базиран на [Keep a Changelog](https://keepachangelog.com/en/1.0.0/).

## [2026-06-17] - Интеграция с онлайн магазини, корекция на отметките и URL адресите, детайлни грешки за Gemini API
### Добавено (Added)
- **Интеграция с онлайн магазини (E-Grocer Platforms) ([SavedRecipes.jsx](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/pages/SavedRecipes.jsx) и [i18n.js](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/i18n.js))**:
  - Реализирахме бутон "Поръчай онлайн" със златист градиент и икона `shopping_bag` в Списъка за пазаруване.
  - Изградихме премиум стъклен модален прозорец "Поръчай продукти онлайн" с избор на верига за хранителни стоки (eBag.bg, Parkmart.bg, Supermag.bg).
  - Имплементирахме автоматично търсене на всеки избран продукт в съответния магазин в нов таб.
  - Добавихме функции за копиране на целия избран списък с количества или само имената за бърза обработка.
  - Създадохме локално състояние `selectedItemsForOrder` за модала за правилна селекция на продуктите за поръчка (отметнатите се търсят/копират, неотметнатите са бледи и изключени).

### Променено (Changed)
- **Миграция към поддържан модел (`gemini-3.5-flash`) ([AIAssistant.jsx](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/pages/AIAssistant.jsx), [gemini_guide.md](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/docs/gemini_guide.md))**:
  - Преминахме от спрения от поддръжка модел `gemini-1.5-flash` към новия и активен към юни 2026 г. модел **`gemini-3.5-flash`** в API заявките и актуализирахме придружаващата документация и често задаваните въпроси (FAQ).
- **Детайлно прихващане на грешки в Chef AI ([AIAssistant.jsx](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/pages/AIAssistant.jsx))**:
  - Обновихме `sendToGemini` и `handleLocalFallbackResponse` да разпознават и показват детайлните грешки от Google Gemini API (напр. грешен ключ, неактивна услуга) или мрежови грешки (като CORS блокаж или липса на интернет връзка). Това улеснява диагностицирането на проблеми с ключа от страна на потребителя.

### Поправено (Fixed)
- **Корекция на линковете за търсене в супермаркетите ([SavedRecipes.jsx](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/pages/SavedRecipes.jsx))**:
  - **eBag.bg**: Премахнахме езиковия префикс от URL (`/search?q=...`), тъй като причиняваше 404 Unexpected Application Error при зареждане на метаданните в SPA на eBag.
  - **Parkmart.bg**: Върнахме адреса към основния домен (`parkmart.bg`), тъй като поддомейнът `sofia.parkmart.bg` не съществува в DNS и хвърляше NXDOMAIN грешка.
  - **Supermag.bg**: Променихме адреса към новия SEO формат на сайта (`/search?q=...`), тъй като старият маршрут с `index.php` връщаше `File not found` поради миграция на платформата.
- **Изчистване на API ключ в състоянието ([AIAssistant.jsx](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/pages/AIAssistant.jsx))**:
  - Фиксирахме пропуск в `handleSaveApiKey`, при който въведеният API ключ се записваше изчистен от празни пространства в `localStorage`, но не и в състоянието (React state). Сега промените се отразяват мигновено и в state-а при запис.
  - Премахнахме неизползвана променлива в `catch` блока на `AIAssistant.jsx`, с което решихме грешка от ESLint статичния анализ.

## [2026-05-31] - Автоматична ротация на Акцент на деня, Реален AI Асистент и Интерактивно Ръководство
### Добавено (Added)
- **Реален AI Асистент ([AIAssistant.jsx](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/pages/AIAssistant.jsx))**:
  - Интеграция с Firestore за зареждане на реални рецепти и съставки.
  - Динамично филтриране по хранителни предпочитания (диетологичен профил, алергени и изключени храни).
  - Премиум чат интерфейс (Chef AI) с интерактивни бързи бутони (Prompt Chips) и автоматично разпознаване на препоръчани рецепти от базата данни за визуализиране като кликаеми карти.
  - Поддръжка на собствен Gemini API Key (съхраняван в `localStorage`) с цел предотвратяване на 403 грешки от ограничения в проектния ключ.
  - Вграден офлайн режим (Gourmet Rule Engine Fallback), който емулира работата на AI кулинарен асистент с реални рецепти при грешка или липса на ключ.
  - **Динамичен Markdown Link Parser в чата**: Добавена е поддръжка за рендериране на стандартни markdown линкове `[текст](цел)` с поддръжка на действие `action:help` за отваряне на интерактивното ръководство.
  - **Интерактивна връзка в офлайн съобщението**: Самият израз "връзка с Gemini API" в приветствения офлайн текст е превърнат в интерактивен златист линк, който при клик отваря директно модала за помощ (`HelpGuideModal`) с настройките и ръководството.
  - **Самостоятелен интерактивен модал за помощ (`HelpGuideModal`)**: Красив и просторен модал с 4 стъпки с икони за регистриране на личен ключ и секция с често задавани въпроси (FAQ) с акордеони на два езика (BG/EN).
  - **Бутон за помощ ❓ в хедъра**: Добавен нов бутон с икона `help` до настройките за незабавно отваряне на ръководството.
  - **Линк в настройките**: Променен е inline акордеонът за помощ в настройките ⚙️, така че да отваря пълноценния модал за помощ и да затваря формата за настройки, решавайки проблема с преливането и скриването на текстове под менюто.
- **Бързи връзки към AI Асистента в UI**:
  - Насочихме бутона *"Опитайте AI"* на началната страница ([Home.jsx](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/pages/Home.jsx)) да отваря реалния `/ai-assistant` вместо стария екран `/ai-search`.
  - Добавихме нов златист бутон *"Chef AI"* с икона `auto_awesome` в хедъра на екрана за Килер ([Pantry.jsx](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/pages/Pantry.jsx)) за лесно отваряне на чат асистента.
- **API Конфигурация ([.env.example](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/.env.example) и [.env.local](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/.env.local))**: Добавена е променливата `VITE_GEMINI_API_KEY`.
- **Подробно Ръководство ([gemini_guide.md](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/docs/gemini_guide.md))**: Изцяло разписано двуезично (BG/EN) текстово ръководство за потребителите, съдържащо стъпки, често задавани въпроси (FAQ) и разрешаване на проблеми с грешка 403.

### Променено (Changed)
- **Разширяване на AI чат балоните ([AIAssistant.jsx](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/pages/AIAssistant.jsx))**: Увеличихме максималната широчина на отговорите на Chef AI до `w-full` (100% от контейнера), за да разширим текстовите блокове и препоръчаните рецепти почти до самите десни очертания на екрана (оставяйки само около 2-5% отстъп заради вътрешния padding `p-4` на чат списъка). Това допълнително подобрява четимостта на съдържанието и рецептите, като същевременно поддържа фиксираното ляво подравняване до аватара на асистента.
- **Подредба на бързите бутони в Чат Асистента ([AIAssistant.jsx](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/pages/AIAssistant.jsx))**: Преструктурирахме бързите бутони (Prompt Chips) в мрежа 2х2 с центрирано подравняване, което прави интерфейса по-подреден и позволява автоматично разширение при добавяне на нови бутони.
- **Алгоритъм за "Акцент на деня" ([Home.jsx](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/pages/Home.jsx))**: Пренаписан е алгоритъмът за избор на акцент, като е заменен статичният избор на най-високо оценената рецепта от малък пул с ежедневна ротация. Сега се извличат до 100 рецепти, сортират се стабилно по ID на устройството и се избира индекс на базата на дневен хеш (от датата в YYYY-MM-DD формат). Това гарантира стабилен, променящ се всеки ден избор, еднакъв за всички потребители и устройства.

### Поправено (Fixed)
- **Мобилно „плуване“ и размери на модалите ([index.css](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/index.css), [AIAssistant.jsx](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/pages/AIAssistant.jsx), [Pantry.jsx](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/pages/Pantry.jsx), [RecipeDetail.jsx](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/pages/RecipeDetail.jsx), [GDPRConsent.jsx](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/components/GDPRConsent.jsx))**:
  - Добавихме глобално ограничение `overflow-x: hidden` и `max-width: 100%` на `html, body` за предотвратяване на хоризонтално скролиране на целия изглед на телефона.
  - Конфигурирахме всички overlays на модалните прозорци (Settings, Help Guide, Add/Edit Pantry, Confirmation, GDPR Consent) да се съобразяват с широчината на приложението (`max-w-md mx-auto w-full`), така че на десктоп да не излизат извън колоната на мобилното приложение, а на телефон да се центрират идеално в екрана.
  - Намалихме максималната широчина на модалната карта за ръководството (`HelpGuideModal`) от `max-w-lg` до `max-w-sm` за консистентност с останалите настройки.
  - Увеличихме z-index на модалните прозорци на `z-[100]`, за да се изобразяват над долното системно меню (`z-50`) и да се избегне покриването или застъпването на техните бутони и форми.

## [2026-05-30] - Поправка на Дневника и Бекъп & Възстановяване, филтриране и нов бутон в Килера
### Добавено (Added)
- **Текстов бутон за добавяне в Килер ([Pantry.jsx](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/pages/Pantry.jsx))**: Добавен е допълнителен, по-видим текстов бутон "Добави" / "Add" в хедъра на страницата, непосредствено до надписа "Твоят Килер" (или "Your Pantry") за подобряване на използваемостта и бързо отваряне на модала за добавяне.

### Поправено (Fixed)
- **Поправка на алгоритъма за автоматично класифициране на тагове на рецептите ([recipeMetaUtils.js](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/lib/recipeMetaUtils.js) и [ManageRecipes.jsx](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/pages/admin/ManageRecipes.jsx))**:
  - Идентифициран и разрешен е проблем, при който рецепти с високопротеинови съставки не се маркират с тага "High-Protein" (`високопротеиново`). Това се дължеше на факта, че базата данни съдържа съставки с български/кирилски тагове (например `"високопротеинова"`, `"веган"`, `"суперхрана"`, `"кето"`), докато алгоритъмът за проверка в `getRecipeTags` правеше директно сравнение само с английските наименования (`"high-protein"`, `"vegan"`, `"superfood"`, `"keto"`).
  - Внедрена е таблица с езикови синоними/алиаси (`TAG_ALIASES`), покриваща българските и английските вариации за всички диетични категории (`vegan`, `vegetarian`, `keto`, `pescatarian`, `gluten-free`, `superfood`, `high-protein`).
  - Подобрено е напасването на съставките по ID (`ingredient_id` / `id` / `slug`) с добавена case-insensitive обработка, trim на празни пространства и fallback сравнение на слъговете.
  - Добавена е защитна проверка в модула за администрация на рецепти при натискане на бутона "Тагове" (`handleRecalculateTags`), предотвратяваща стартиране на операцията преди списъкът със съставки от Firestore да се е заредил напълно.
- **Замяна на таб "Смарт" с таб "Всички" и реорганизация на решетката ([Home.jsx](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/pages/Home.jsx))**:
  - Премахнат е табът "Смарт" и е заменен с функционален таб "Всички", който изобразява до 150 рецепти едновременно (за предпазване от натоварване на системата при голям брой данни).
  - Променена е подредбата на табовете в мрежата в съответствие с поискания 2х2 формат:
    * Ред 1: `[Най-нови]` и `[Най-оценявани]` (преименуван от "Топ оценени").
    * Ред 2: `[Най-гледани]` и `[Всички]`.
- **Разширено търсене и показване на публични версии на Началната страница ([Home.jsx](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/pages/Home.jsx))**:
  - Обновен е филтърът на началния екран, за да се показват и всички одобрени публични персонализирани версии на рецепти (`is_public_variation === true` и `is_active !== false`).
  - Премахнато е ограничението `limit(100)` от Firestore заявката за зареждане на рецепти, което вече позволява търсенето да сканира цялата база данни.
  - Премахнато е ограничението от 10 резултата при активно търсене или избрана филтър-категория (например "Салати", "Супи" и т.н.), като системата вече извежда всички съвпадащи съдържания. Лимитът до 10 елемента остава активен само за началния изглед по подразбиране с цел бързина на интерфейса.
- **Модерация на публични персонализирани версии на рецепти ([RecipeCustomization.jsx](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/pages/RecipeCustomization.jsx))**:
  - Имплементирана е коректна логика за модерация при създаване на "Моя версия" от стандартни потребители. Когато се създава публична версия, тя се записва в базата данни с `is_active: false` и `status: 'pending'`, което я изпраща директно в модула за админ модерация. Частните версии и версиите, създадени от администратори, се одобряват автоматично веднага.
- **Визуален преглед (Thumbnail) на потребителските версии ([RecipeDetail.jsx](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/pages/RecipeDetail.jsx))**:
  - В списъка "Потребителски версии" на детайлния изглед на рецептата е добавено малко медийно изображение (thumbnail) с фини очертания пред името на всяка версия за по-добра забележимост и по-професионален дизайн.
- **Оптимизиране на хедъра в управлението на рецепти ([ManageRecipes.jsx](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/pages/admin/ManageRecipes.jsx))**:
  - Реорганизиран е претрупаният хедър на три отделни реда за по-добра адаптивност и предотвратяване на излизането на информация извън зрителното поле. Първият ред съдържа бутона за връщане назад, заглавието "Рецепти" с общия брой на въведените рецепти и бутоните за превключване на изгледа (Grid/List). Вторият ред съдържа бутоните за действия: "Тагове", "Експорт CSV" и "Импорт CSV" с разширено хоризонтално разпределение. Третият ред съдържа филтрите за статус: "Активни", "Деактивирани" и "Изтрити", разпределени поравно.
- **Нормализиране и обединяване на дублиращи се продуктови категории ([recipeMetaUtils.js](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/lib/recipeMetaUtils.js), [ManageRecipes.jsx](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/pages/admin/ManageRecipes.jsx), [RecipeCustomization.jsx](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/pages/RecipeCustomization.jsx), [Pantry.jsx](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/pages/Pantry.jsx), [Home.jsx](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/pages/Home.jsx), [ManageIngredients.jsx](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/pages/admin/ManageIngredients.jsx))**:
  - Идентифициран и разрешен е проблем с дублиране на категории (като "Зеленчуци" и "VEGETABLES" или "Мазнини" и "FATS"), причинен от несъответствие между българските имена (в семенните данни) и английските ID-та на групите (при въвеждане през UI) в полето `classification.main_group`.
  - Добавена е централизирана помощна логика за нормализация (`normalizeMainGroup` и `getMainGroupLabel`), която обединява групите под единни вътрешни ключове и коректно ги превежда на български или английски в зависимост от избрания език на интерфейса.
- **Филтриране на нативни реклами в глобалния банер ([AdBanner.jsx](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/components/AdBanner.jsx))**:
  - Коригиран е филтърът на активните реклами в глобалния банер, като сега реклами от тип `native` (които са предназначени единствено за контекстно изобразяване в списъка със съставки на рецептите) се изключват от пула на глобалния банер. Това предотвратява счупеното рендериране, при което банерът показваше суровия URL адрес на изображението.
- **Поправка на Дневник на събитията ([ActivityLog.jsx](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/pages/admin/ActivityLog.jsx))**:
  - Въведена е спомагателна функция `safeStr` за безопасно изобразяване на текстовите полета на лога (като `userEmail`, `details`, `action`). Това предотвратява срива на React ("Objects are not valid as a React child"), възникващ при опит за изобразяване на стари невалидни лог записи, записани като обекти (поради грешни сигнатури на `logActivity` в предходни версии).
- **Защита на лог системата ([activityLogger.js](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/lib/activityLogger.js))**:
  - Имплементирана е автоматична проверка за тип в `logActivity`. Ако параметрите се подадат като обекти по грешка, те се сериализират автоматично чрез `JSON.stringify`, осигурявайки съвместимост с Firestore и гарантирайки, че UI никога няма да се счупи в бъдеще.
- **Поправка на Бекап и Възстановяване ([BackupRecovery.jsx](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/pages/admin/BackupRecovery.jsx))**:
  - Коригирани са неправилни имена на колекции (`activity_log` -> `activity_logs`, премахната липсващата `recipe_versions`), което водеше до Permission Denied грешки и срив при локален експорт и облачен архив.
  - Премахната е несъществуващата колекция `user_pantry` от общия списък. Вместо това е разработен динамичен механизъм, който при архивиране на `users` автоматично извлича подколекцията `pantry` за всеки отделен потребител и я прикачва към неговия обект.
  - Пренаписан е процесът на възстановяване (Restore), така че всички операции (включително вложените подколекции `pantry`) се групират и изпълняват на порции от по 400 документа (в границите на 500 лимита на Firestore Batch) за максимална надеждност.
  - Добавена е интеграция с `useAuth`, за да може записването на събития в дневника чрез `logActivity` да става с реалния UID и имейл на извършващия бекъпа администратор.
  - Добавени са колекциите `ads` и `settings` към списъка за архивиране.

### Променено (Changed)
- **Контрол на мерните единици в Килер ([Pantry.jsx](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/pages/Pantry.jsx))**:
  - Имплементирано е филтриране на мерните единици при добавяне и редактиране на продукти в Килера. Сега падащите менюта показват само конкретните мерни единици, дефинирани в `units_mapping` на избрания продукт, вместо целия списък с мерни единици.
  - Въведен е fallback, така че ако за продукта не са конфигурирани специфични мерни единици, падащото меню показва всички налични единици от базата данни.
  - Поддържа се сигурност, при която текущо избраната мерна единица винаги се добавя в списъка с опции, за да се избегнат React грешки при зареждане на по-стари данни.

## [2026-05-28] - Ранг филтриране, Фикс за Дневника и Редизайн на рецепти
### Променено (Changed)
- **Филтриране на потребители в Управление на Потребители (`ManageUsers.jsx`)**:
  - Въведено е ограничение за потребители с роля Модератор (`MODERATOR`) – при избор на Администрация -> Потребители те вече виждат само потребители със същия или по-нисък ранг (Модератор, Потребител). Потребители с по-висок ранг (Администратор, Собственик) се филтрират и скриват.
  - Обновен е и общият брой в хедъра на страницата, така че за Модератори да отразява броя на достъпните за тях потребители вместо общия регистриран брой в базата данни.
- **Подобрения и корекции по екрана "Моя версия" за персонализиране на рецепти (`RecipeCustomization.jsx`)**:
  - Имплементиран е информационен банер в горната част, показващ оригиналния брой порции.
  - Изцяло преработен дизайн на страницата 1-към-1 с административния панел за редактиране на рецепти (`ManageRecipes.jsx`) чрез използване на същите табове (Основна, Съставки, Стъпки) и същия златист контейнер.
  - Пренаредени полета в таб "Основна": текстовите полета за заглавие (BG/EN) са подредени вертикално (едно под друго), а зоната "Направи публична" е разпъната на пълна ширина.
  - Преработка на таб "Съставки" с цел добавяне на продукти директно в табличен вид, пренареждане чрез бутони със стрелки, извеждане на двуезични забележки под съответните продукти и филтриран избор на мерни единици.
  - Преработка на таб "Стъпки" с цел попълване на инструкции за стъпки на двата езика едновременно и прикачване на таймери в минути.
  - Бутонът за запис е интегриран като статичен златист бутон в края на формата.
- **Редизайн на хедъра на рецептите (`RecipeDetail.jsx`)**:
  - Името на рецептата, описанието, таговете и интерактивната оценка са изнесени извън (под) снимката на рецептата, осигурявайки по-чист и лесен за четене изглед.
  - Шрифтът на името на рецептата е намален с 10% (от `text-4xl` на `text-3xl`).
  - Премахнато е голямото цялостно затъмнение върху снимката и е заменено с по-малка градиентна зона в долната част на снимката, покриваща само 30% от нейната височина (transition към тъмния фон).
- **Валидация за дублиращи се продукти при пазаруване (`RecipeDetail.jsx`)**:
  - Добавена е валидация при натискане на бутона "Добави липсващите в списъка". Системата проверява за съвпадащи съставки в списъка по ID на продукт.
  - Въведен е нов диалогов прозорец (Modal) с премиум тъмен дизайн за повтарящите се продукти, показващ текущото количество, добавяното количество и изчисленото ново общо количество.
  - Потребителят може да отметне чрез чекбоксове (всички активни по подразбиране) кои продукти да добави допълнително или кои да откаже.
  - Потвърждението на дублиращите се продукти автоматично увеличава количеството за съществуващия продукт на място, вместо да добавя нов ред. Нови редове се добавят само за изцяло нови съставки. Отказът в диалоговия прозорец спира операцията.
- **Трансформация и интелигентно автоматично конвертиране на мерни единици в Списъка за Пазаруване (`RecipeDetail.jsx` и `SavedRecipes.jsx`)**:
  - При добавяне на съставки към списъка за пазаруване, течните продукти (с `meta.is_liquid === true` в БД) се преобразуват в `ml`, а твърдите продукти (с `meta.is_liquid !== true` в БД) – в грамове (`g`), ако е налична тегловна/обемна конверсия.
  - Премахнато е ръчното превключване на единици (`swap_horiz`). Вместо това, в списъка за пазаруване ([SavedRecipes.jsx](file:///c:/Users/KAMEH%20Y3YHOB/Documents/GitHub/the-best-idea-eatery/src/pages/SavedRecipes.jsx)) се извършва напълно автоматично преобразуване на визуализираните количества при преминаване на кота от 500 гр./мл. (показват се като `kg`/`l` при над 500 и съответно `g`/`ml` при 500 или по-малко).
  - При съвпадение на дублиращи се продукти, системата извършва интелигентно нормализиране на единиците в базов формат (`g`/`ml`) преди сумирането, за да се предотвратят математически грешки.
- **Подобряване на адаптивността и мобилния изглед в Списъка за Пазаруване (`SavedRecipes.jsx`)**:
  - Отстранено е претоварването/преливането (overflow) отдясно на екрана при по-тесни устройства и при десктоп благодарение на премахването на `md:flex-row` за формите за добавяне на продукти и редовете в режим на редактиране (Edit Mode).
  - Хедърът на списъка е обновен с `flex-wrap` and `gap-3`, за да може заглавието и бутоните за управление ("Изтрий списъка" и "Редактирай") автоматично да се пренасят на нов ред при недостиг на пространство, предотвратявайки частичното им скриване извън екрана.
  - Ръчно добавяне и редактиране на продукти вече са с оптимизиран двуредов изглед (Име на отделен ред, Количествени контроли и бутон на втори ред с гъвкави/разтегливи размери на Unit селектора), осигурявайки 100% четимост и достъпност на бутоните на всички устройства.
- **Превод на мерни единици на български език (`SavedRecipes.jsx` и `Pantry.jsx`)**:
  - Имплементирана е сигурна система за превод/fallback за стандартните мерни единици (`g` -> `гр.`, `kg` -> `кг.`, `ml` -> `мл.`, `l` -> `л.`, `pcs` -> `бр.`), която гарантира, че те се изписват правилно на български език във View и Edit режимите на Списъка за пазаруване и Килера, дори ако не са заредени документи в колекцията `measurements`.
- **Премахване на продукти директно от списъка във View режим (`SavedRecipes.jsx`)**:
  - Добавен е нов бутон за бързо изтриване (икона за кофа за боклук `delete`) на отделните продукти директно във View режим (вдясно от стрелките за пренареждане) с искане за потвърждение (Confirm prompt).
  - При изтриване списъкът с отметнати продукти (`checkedItems`) се актуализира автоматично, като съответно се преместват индексите на останалите продукти за запазване на коректни отметки.
- **Коригиране на Firestore правила за достъп (Security Rules) за Килер (`firestore.rules` и `firebase.json`)**:
  - Създаден е нов локален файл `firestore.rules`, описващ правата за достъп до всички Firestore колекции.
  - Дефинирано е специфично правило за подколекцията `users/{userId}/pantry/{pantryId}`, което разрешава четене и писане за собственика на профила (`request.auth.uid == userId`) и за администраторите. Това отстранява проблема, при който регистрирани потребители и модератори не можеха да добавят продукти в Килера поради липсващо правило за подколекции в облака.
  - Интегриран е блокът `"firestore"` във `firebase.json` и е обновен скриптът за качване (`upload.bat`) да изпълнява пълна команда `firebase deploy`, за да може автоматично да се обновят и качат правилата за сигурност в Firestore при следващ деплоймент.
- **Разместване и пренареждане на продукти в Списъка за Пазаруване (`SavedRecipes.jsx`)**:
  - Имплементирана е функционалност за промяна на реда (разместване) на продуктите с бутони за стрелки нагоре/надолу.
  - Разместването е достъпно както в режим на редактиране (Edit Mode), така и директно във View режим за по-удобно и бързо пренареждане по време на пазаруване.
  - Бутоните за разместване са преместени и позиционирани **след** количеството и мерната единица във View режим и Edit Mode, с оправени размери и отстояния за предотвратяване на грешно кликване.
  - Осигурено е пълно синхронизиране на списъка с отметнати (купени) продукти при разместване (размяна на индексите) и при влизане/излизане от Edit Mode.
- **Облачна синхронизация на Списъка за Пазаруване (`AppContext.jsx`)**:
  - Имплементирано е автоматично синхронизиране на списъка за пазаруване в Firestore (`users/{userId}` в полето `shopping_list`) за логнати потребители в реално време.
  - За гости списъкът се съхранява в `localStorage` с асинхронно зареждане за предотвратяване на регенериране на цикли.
- **Бутон "Изтрий списъка" с потвърждение (`SavedRecipes.jsx`)**:
  - Добавен е нов червен бутон в хедъра на списъка за изчистване на всички продукти със запитване за потвърждение (Confirm prompt) преди изпълнението.
- **Ръчно добавяне на персонални продукти в Списъка за Пазаруване (`SavedRecipes.jsx`)**:
  - Добавена е хоризонтална форма за текстово въвеждане на продукт, количество и мярка (`бр.`, `гр.`, `кг.`, `мл.`, `л.`).
  - Формата се показва под банера за празен списък (за ръчно изграждане) и в долната част на списъка, когато има съдържание.
  - Персоналните продукти се синхронизират успешно с Firestore/localStorage и поддържат пълно редактиране и пренареждане.

### Поправено (Fixed)
- **Отчитане на потребителски имейл в Дневника при работа с Реклами (`ManageAds.jsx` и `ActivityLog.jsx`)**:
  - Поправена е грешка в извикването на `logActivity`, където се търсеше несъществуващият ключ `user.auth?.email` вместо `user.email`. Това причиняваше записването на имейл "N/A" в Дневника на събития при действия с реклами, рекламни кампании и правила за реклама. Сега имейлите на логнатите администратори се записват коректно.
- **Интелигентни и динамични тагове по рецептите (`recipeMetaUtils.js`, `RecipeDetail.jsx`, `Home.jsx`, `SavedRecipes.jsx`)**:
  - Поправена е поддръжката за съвпадение на съставки: сега се поддържат всички формати на ключове за ID на съставка (`ingredient_id`, `ingredientId` и `id`), което позволява правилна работа както при нови рецепти, така и при seeded/mock данни.
  - Направено е изцяло регистронезависимо (case-insensitive) сравнение на таговете, за да може тагове като `High-Protein` или `Vegan` в базата данни да се разпознават коректно, дори и да са въведени с главни букви.
  - Прехвърлено е изчисляването на таговете изцяло динамично на клиента (със запазване на статичния fallback при първоначално зареждане), така че рецептите в детайлния изглед, началния екран и запазените рецепти веднага да се обновяват при промяна на таговете на продуктите в базата данни.

## [2026-05-26] - Редактиране и локализиране на Списъка за Пазаруване (Shopping List)
### Добавено (Added)
- **Бутон "Редактирай" и инлайн Edit Mode (`SavedRecipes.jsx`)**:
  - Добавен е бутон „Редактирай“ (заедно със съответно „Запиши“ и „Отказ“) в Списъка за пазаруване.
  - Имплементиран е инлайн режим за редактиране на целия списък: потребителят може да променя името на продукта (текстово поле), количеството (числово поле) и мерната единица (падащо меню) за всеки елемент паралелно, както и да изтрива редове с иконата за кофа за боклук.
  - Бутонът за записване актуализира състоянието в глобалния контекст чрез `setShoppingList`.
- **Интерактивно отмятане на купени продукти**:
  - Чекбоксовете вляво от продуктите вече са напълно функционални. Щракването върху тях превключва „купено“ състояние, което визуално зачерква името на продукта и намалява неговата непрозрачност (opacity).
- **Локализиран избор на мерни единици**:
  - Вътре в падащото меню за избор на единица в Edit Mode се показват локализираните имена на единиците.

### Поправено (Fixed)
- **Липсващи имена и нелокализирани мерни единици**:
  - Поправено е показването на имената на продуктите (преди се връщаше празно или `undefined` при Български език, тъй като се търсеше несъществуващият ключ `nameBg`).
  - Поправено е изобразяването на единиците на продуктите в списъка (преди се показваха като сурови системни ID-та от Firestore, напр. `'g'`, а сега се извличат локализираните им имена).

## [2026-05-26] - Реконструкция на списъка и редактирането в Килер (Pantry)
### Добавено (Added)
- **Edit Modal в Килера (`Pantry.jsx`)**:
  - Създаден е нов Edit Modal с премиум glassmorphic дизайн и син акцент.
  - Позволява пълно редактиране на количество, мярка и срок на годност за продуктите в килера.
  - Името на продукта се визуализира като неактивно (read-only), както е по задание ("с изключение на името на продукта").
  - Списъкът с мерни единици се извлича динамично от `units_mapping` на продукта, като се комбинира с глобалните дефиниции.
- **Динамично изчисляване на срок на годност**:
  - При избор на съставка при добавяне в килера, срокът на годност автоматично се изчислява като `текуща дата + среден срок на годност` (`average_shelf_life_days`) от базата с продукти.
  - По подразбиране се задават 7 дни, ако в базата данни липсва стойност за среден срок на годност.
  - Потребителят може да променя автоматично попълнената дата на годност в модала.

### Променено (Changed)
- **Дизайн и групиране на продуктите в Килера**:
  - Списъкът с налични продукти в килера вече прилича на модула "Продукти" от администраторския панел (`ManageIngredients.jsx`).
  - Продуктите са групирани по категории с прилежаща икона, заглавие и разделителни линии.
  - Използват се компактни карти с кръгли икони (`size-10`), цветен бедж за количеството, изчистено състояние за срок на годност и фин цветен индикатор за времето до изтичане.
  - Инлайн редактирането е заменено с постоянно видими бутони за редакция (отварящ Edit Modal) и изтриване (с потвърждение).

## [2026-05-26] - Отделяне на Диетичния Профил в самостоятелен екран
### Добавено (Added)
- **Страница за редактиране на Диетичен Профил (`DietaryProfileEdit.jsx`)**:
  - Нов екран, позволяващ пълноценно редактиране на диетичните предпочитания, алергени и изключени храни.
  - **Английски езикови филтри**: Добавено изрично указание в подсказките (placeholders) и описанията, че стойностите за „Диети“ и „Алергии“ трябва да се попълват **само на английски език**, за да съответстват на таговете на продуктите в базата данни.
  - **Падащо меню за изключени храни**: Заменихме свободното текстово въвеждане на изключени храни с интелигентно търсене (autocomplete) от глобалната база с продукти `ingredients`.
  - **Записване на ID**: Програмата вече съхранява само уникалните идентификатори (ID) на изключените съставки.
  - **Визуализация на списък**: Избраните продукти за изключване се визуализират в красив вертикален списък (един под друг) с локализираното им име и бутон за бързо премахване (`close`).
  - Поправен е рендерингът на иконата за изключени храни чрез замяна на `do_not_distribute` със стандартната и широко поддържана икона `block`.
- **Маршрутизация**: Добавен нов защитен маршрут `/pantry/diet` in `App.jsx`.

### Променено (Changed)
- **Интерфейс на Килера (`Pantry.jsx`)**: Разделът „Моят Диетичен Профил“ е заменен с бърз бутон „Редактирай“, препращащ към новия екран, олекотявайки кода на килера.

## [2026-05-26] - Конвертиране на мерни единици при съпоставяне с Килера
### Добавено (Added)
- **Зареждане на мерни единици**: В началната страница (`Home.jsx`) се зареждат в реално време дефинираните в базата данни мерни единици (`measurements`).

### Поправено (Fixed)
- **Конвертиране на мерни единици за коректно сравнение**:
  - Имплементирана е логика с помощни функции `convertToGrams` и `convertFromGrams` в `RecipeDetail.jsx` и `Home.jsx`.
  - При сравняване на наличните в Килера продукти спрямо нужните за рецептата количества, стойностите се конвертират към грамове (или милилитри/еквиваленти) на базата на коефициентите от Firestore (`to_g_average` / `to_ml`), вместо да се сравняват суровите числа (напр. сравняване на 1 кг в килера с 250 г в рецептата вече работи правилно).
  - Стойността на липсващото количество за купуване (`quantityToBuy`) се конвертира обратно в оригиналната мерна единица на рецептата за правилна визуализация и добавяне в списъка за пазаруване.

## [2026-05-26] - Поправка на линтинг грешки и логика за съвпадение в килера
### Поправено (Fixed)
- **Изчистване на линтинг грешки (ESLint)**:
  - Премахнати са предупрежденията за синхронно извикване на `setState` в `useEffect` куки в `src/context/AppContext.jsx`, `src/pages/RecipeDetail.jsx` и `src/pages/admin/ManageUsers.jsx` чрез пренасочването им през `setTimeout`.
  - Отстранени са неизползвани променливи `e` и празни блокове в catch изразите в `src/pages/RecipeDetail.jsx`.
- **Логика за съвпадение на продукти в Килера**:
  - Имплементирано е съвпадение на съставките **изцяло по ID**, вместо по имена (както бе заявено от потребителя).
  - Логиката вече проверява съвпадение по ID, като поддържа както `p.ingredientId` / `p.ingredient_id` (Firestore колекции в камилски/змийски кейс), така и `p.id` / `reqIng.id` за съвместимост с mock данни и seeded записи.
  - Премахнати са всички сравнения по име, за да се избегнат проблеми с несъответствие при изписване в рецептите и килера (напр. "Картофи" спрямо "Картоф").

## [2026-05-24] - Килер (Pantry) и Нативни Реклами
### Добавено (Added)
- **Интеграция на "Килер" с Firestore**: Продуктите в килера вече се запазват перманентно в базата данни (`users/{uid}/pantry`) и са достъпни от всяко устройство.
- **Интелигентно търсене в Килера**: При добавяне на нов продукт е внедрена търсачка с падащо меню (autocomplete), която извлича съставките директно от главната база `ingredients`, заедно с техните категории и мерни единици.
- **Управление на продукти (CRUD)**: Добавена е възможност за бързо редактиране на наличното количество (inline edit) и изтриване на продукти от килера.
- **Диетичен Профил в Килера**: Създадена е нова секция "Моят Диетичен Профил" в горната част на Килера за управление на диети, алергии и изключени храни. Данните са синхронизирани в реално време с настройките на потребителския профил (`user.preferences`).
- **Контекстна (Нативна) Реклама в Рецептите**: Внедрен е нов рекламен формат `native`. Рекламите се показват дискретно (с етикет "Спонсорирано") директно в списъка със съставки на рецептата, ако ключова дума от рекламата съвпада с името на съставката. Включено е и отчитане на импресии и кликове във Firestore.
- **Покани за Модератори и Администратори**: Създаден е нов процес, при който Собственикът може да изпраща покана на потребители за повишаване в ранг (към Модератор или Администратор) от профила им в `ManageUsers.jsx`.
- **Приемане на покана**: Потребителите виждат изпратената до тях покана в личния си профил (`ProfileSettings.jsx`) под формата на бутон. При приемане ролята им се променя автоматично.
- **Филтриране на Дневника на действията по имейл**: Имейлът на потребителите в Дневника вече е кликваем и автоматично филтрира записите само за този потребител. Добавен е и бърз бутон-препратка до имейла в `ManageUsers.jsx`, който отваря Дневника с приложен филтър.

### Поправено (Fixed)
- **Firebase Правила (Security Rules)**: Създаден е локален файл `storage.rules`, който дефинира грануларни права за достъп до снимките във Firebase Storage спрямо ролите на потребителите (профили, аватари, рецепти, реклами и бекъпи). Правилата са вързани с `firebase.json` за лесно качване.
- **Firestore Правила за Реклами**: Добавени са липсващи права за четене (read access) към колекцията `settings`, което поправи грешката "Възникна грешка при зареждане на съдържанието" в публичната страница за реклама.
- **Килер (Pantry) UI**: Коригирано е позиционирането на плаващия бутон за добавяне на продукти (`+`), който на широки екрани излизаше извън рамките на мобилния изглед и ставаше невидим за потребителя.
## [2026-05-23] - Качване (Deployment)
### Добавено (Added)
- **Deployment**: Извършено успешно компилиране (`build`) и качване на проекта в **GitHub** и **Firebase Hosting**.

## [2026-05-22] - Модул "Реклама", Дневник на действията и Правила
### Добавено (Added)
- **Модул Реклама (Правила)**: В административния панел `ManageAds.jsx` е добавена възможност за създаване и редактиране на страницата "Правила за рекламиране при нас" на два езика с HTML поддръжка. Добавен е бутон за бързо копиране на линка.
- **Публична страница за реклама**: Създаден е нов публичен изглед `AdvertiseInfo.jsx` (достъпен на `/advertise`), който показва въведените правила на съответния език с богат и изчистен дизайн.
- **Дневник на действията (Реклами)**: Интегрирано е автоматично записване (`logActivity`) на всички действия в модул Реклами: създаване на кампания, добавяне, редакция, триене на реклами и запазване на правила.

### Поправено (Fixed)
- **Firebase Правила за достъп (Security Rules)**: Изцяло обновени и оптимизирани правила за Firestore, които включват всички липсващи колекции (`activity_logs`, `measurements`, `ingredient_groups`, `system_history`, `system_backups`, `pantry`, `saved_recipes`).
- **Дневник на действията (Activity Log Bug)**: Поправена е грешка във функцията `logActivity`, която "преглъщаше" `undefined` стойности за имейл при изчистване на дневника, дължаща се на скорошна промяна в структурата на потребителя (`user.auth.email`). Вече се ползват сигурни fallbacks.

## [2026-05-22] - Визуализация на цени и тагове по рецептите
### Добавено (Added)
- **Най-използвани 12 продукта**: В началната страница (`Home.jsx`), точно под списъка с рецептите, е добавен стилен интерактивен бутон. При натискане се визуализира мрежа (grid) с 12-те най-срещани съставки в приложението и точния брой рецепти, в които участват.
- **Тагове на база продукти**: Създадена е логика (`recipeMetaUtils.js`), която автоматично разпознава и показва диетични характеристики (Vegan, Keto, Superfood, High-Protein и др.) на рецептата, ако съставките ѝ отговарят на условията.
- **Ориентировъчна цена**: Създаден е `priceUtils.js`. В картите с рецепти (`Home.jsx`) и в самите рецепти (`RecipeDetail.jsx`) вече се изчислява и показва цена за 1 порция.
- **Превод на кухните**: Заместено е показването на системното име на кухнята (slug_id) с локализираното му човешко име (на бълг. или англ.) в `RecipeDetail.jsx` и картите в `Home.jsx`.

### Променено (Changed)
- **Глобален Рекламен Банер**: Рекламният банер (`AdBanner.jsx`) е преместен от началната страница на глобално ниво в `App.jsx`, за да се визуализира в долния край на **всяка** страница в приложението.
- **Ограничение за реклами**: Добавена е логика рекламният банер да се показва **единствено** на потребители със статут "Гост" или "Потребител". За администратори, модератори и собственици той е скрит.
- Премахнати са външните линкове (badges) "Сайт/Site" от `Home.jsx` по желание на възложителя.

## [2026-05-20] - Ролеви достъп (RBAC) за Модератор и Потребител
### Добавено (Added)
- **Мулти-изображения в рецепти (Слайдер / Галерия)**:
  - Имплементирана е нова визуална система за разглеждане на няколко снимки към дадена рецепта в `RecipeDetail.jsx`.
  - При наличие на повече от едно изображение се показва дясна вертикална колона от кръгли thumbnails с фини златни граници около активното изображение и плавен hover ефект за неактивните.
  - Добавени са полупрозрачни навигационни бутони (chevrons) върху основната снимка за бързо и интуитивно прелистване на изображенията наляво и надясно с циклично въртене.
- **Източник на вдъхновение и Видео линкове**:
  - Добавена е нова визуална секция в детайлите на рецептата (`RecipeDetail.jsx`), която извежда елегантни и стилизирани линкове към YouTube/Vimeo видео, име на оригиналния автор и връзка към уебсайт (ако са въведени).
  - В списъчния изглед на началната страница (`Home.jsx`), както и при рецептата "Акцент на деня", са интегрирани информативни значки (badges) с икони, показващи с един поглед наличието на видео рецепта или външен източник.
- **Изчистване на линтинг грешки и предупреждения**:
  - Отстранени са грешки за недефинирани и неизползвани променливи в `ManageAds.jsx` (дефиниран `isBg` чрез `useTranslation`, премахнат неизползваният `user`).
  - Оптимизиран е `AdBanner.jsx` чрез премахване на неизползваните импорти `where` и `orderBy`, както и неизползваните аргументи в `catch`.
  - Почистена е страницата `Login.jsx` от неизползваната променлива и функция за състояние `showGdprWarning`.
  - Премахнати са неизползваните импорти в `RecipeCustomization.jsx` and `BackupRecovery.jsx` (`limit as firestoreLimit`).
  - Добавена е липсващата зависимост `awardPoints` в Hook масива на `RecipeDetail.jsx`.
- **Бързи филтри по категория (бутони) на началната страница (`Home.jsx`)**:
  - Добавен е динамичен списък от бутони (Chips), подредени на няколко реда с автоматично обвиване (wrap), под заглавието на рецептите за всички основни категории в проекта: Салати, Супи, Предястия, Основни, Десерти, Тестени, Напитки, Сос/Марината, Закуска, Специален повод.
  - Бутоните показват икони и съответните езикови еквиваленти в зависимост от избрания език на потребителя (Български или Английски) и използват естествено множествено число.
  - Реализирано е клиентско филтриране в `useEffect` при щракване на бутон за ограничаване на визуализирания списък само до рецепти от съответната категория (`category_id`).
  - Активният филтър се визуализира в заглавието на списъка (напр. `Най-нови • Салати`) и е лесно разпознаваем благодарение на открояващ се златист цвят.
- **Двуезично търсене на рецепти по име и съставка**:
  - Въведено е динамично търсене на началната страница на приложението (`Home.jsx`), което филтрира рецепти не само по тяхното заглавие (`title_bg`/`title_en`), но и по името на всяка от съставките им (съпоставяйки ги с въведената фраза за търсене).
  - Търсенето проверява както локално съхранените имена на съставки в самата рецепта, такa и имената в глобалната колекция `ingredients` от Firestore (на български и английски език).
  - **Оптимизирано разположение на търсачката**: Търсачката е преместена под "Акцент на деня" и непосредствено над категориите, за да бъде визуално по-близо до резултатите и по-лесна за разпознаване.
- **Запазване на имена на съставки в базата данни**:
  - Редактиран е модулът `ManageRecipes.jsx`, така че при записване или актуализация на рецепта, имената на съставките (`ingredient_bg` и `ingredient_en`) се извличат автоматично от списъка на базата и се записват в обекта на рецептата в Firestore, осигурявайки съвместимост с всички останали страници.
- **Пренареждане на съставки**:
  - Добавена възможност за пренареждане на продуктите в рецепта при въвеждане или редактиране чрез вертикално подредени триъгълни стрелки ("нагоре" и "надолу") за всяка съставка, разположени на втория ред под поредния номер и вляво от бележките.
- **Динамично преизчисляване на съставки спрямо порции**:
  - Добавено автоматично пропорционално мащабиране на количествата на всички съставки при промяна на порциите във формата с цел запазване на нормализираните им стойности за 1 порция в базата данни.
- **Изображение-заместител за рецепти (Placeholder)**:
  - Генерирано и внедрено ново висококачествено изображение-заместител в `public/images/recipe-placeholder.png` за рецепти, които не разполагат с дефинирани от потребителя снимки.
  - Конфигурирани коректни fallback пътища във всички страници, показващи рецепти (`Home.jsx`, `RecipeDetail.jsx`, `AIAssistant.jsx`, `ManageRecipes.jsx`), с цел отстраняване на счупени линкове и икони.
- **Физическо изтриване на рецепти от Собственика**:
  - Добавена възможност за Собственика (`ROLES.OWNER`) да изтрива логически изтрити рецепти окончателно (физически) от Firestore базата с данни чрез допълнителен бутон "Изтрий завинаги", видим само за него при преглед на секцията "Изтрити".
  - Преди изтриване се извършва двуезично потвърждение от браузъра и автоматично се записва последна архивна версия на рецептата с маркер `PHYSICAL_DELETE` за проследимост.
- **Модераторски достъп**:
  - Модераторите имат само права за четене (Read-only) в управлението на потребители (`ManageUsers.jsx`). Забранени са операции за смяна на роля, активност, триене/възстановяване и редактиране на профили.
  - Модераторите виждат "Рецепти" и "Продукти" в Data Dashboard, но нямат достъп до групи съставки и мерни единици.
  - В управление на рецепти (`ManageRecipes.jsx`), модераторите виждат всички рецепти (включително изтритите), но не могат да трият, възстановяват или сменят активния статус директно.
- **Потребителски достъп**:
  - Потребителите (`ROLES.USER`) имат достъп до Data Dashboard и `ManageRecipes.jsx`, но виждат само собствените си рецепти (филтрирани по `publisher_id`).
  - При създаване или редактиране на рецепта от стандартен потребител, рецептата автоматично се маркира с `status = 'pending'` и `is_active = false` за последваща модерация.
  - Потребителите не виждат таба "Изтрити" рецепти и не могат да сменят статуса на рецепти.
- **Модул Модерация**:
  - При одобрение на чакащо съдържание в `Moderation.jsx`, статусът се сменя на `'approved'`, а рецептата се активира автоматично (`is_active = true`).
  - При отхвърляне статусът става `'rejected'`, а рецептата остава неактивна.
  - Добавена поддръжка за визуализация на двуезични полета (заглавия, описания) и име на автор/издател с интелигентни fallbacks.

### Поправено (Fixed)
- **Имена на съставки при по-стари/seeded рецепти**: Въведено е извличане и търсене на имена на съставки от глобалната колекция `ingredients` в `RecipeDetail.jsx` като подсигуряващ fallback, ако рецептата няма вградени стойности `ingredient_bg`/`ingredient_en`.
- **Изчистване на неизползвани променливи**: Отстранени са неизползвани импорти и дефиниции на променливи в `Home.jsx` (`isGuest`, `isPantryActive`, `missing`, `isReady`, `generateShoppingList`) и `RecipeDetail.jsx` (`t`, `description`) за пълно съответствие с изискванията на линтера.
- Премахнат неизползван `getDoc` от `ManageUsers.jsx`.
- Поправени празни catch блокове в `ManageRecipes.jsx` чрез логване на предупреждение.

## [2026-05-15] - GDPR Съответствие и Поверителност
## [2026-05-15] - GDPR Съответствие, Рекламна система и Поверителност
### Добавено (Added)
- **GDPR Модул**:
  - Нов компонент `GDPRConsent.jsx` за глобално съгласие при първо влизане.
  - Персистиране на съгласието в `localStorage` за регистрирани потребители и `sessionStorage` за гости.
  - Страници **Общи условия** (`TermsOfService.jsx`) и **Политика за поверителност** (`PrivacyPolicy.jsx`) на два езика.
- **Рекламен Модул**: Пълна административна система за управление на кампании, банери и статистика.
- **Динамичен Акцент**: Автоматично извличане на най-високо оценена рецепта за началната страница.
- **Стабилност за Гости**: Корекции на права за достъп и обработка на грешки при неоторизиран достъп.
- **Smart Pantry**: Превключвател за видимост и логика за скриване на пазарни функции за гости.
- **Сигурност и акаунти**:
  - Задължително съгласие с правилата при регистрация на нов потребител.
  - Интегриран механизъм за "отключване" на GDPR функции директно от страницата за вход.
  - **Изтриване на профил**: Пълна функционалност за изтриване на акаунт с двойно потвърждение.
  - **Анонимизиране на съдържание**: При изтриване на профил, неговите рецепти остават в системата, но авторът им се сменя на "Deleted User / Анонимен готвач".
- **Локализация**:
  - Пълна поддръжка на преводи за правната секция в `i18n.js`.
- [2026-05-15] **Рекламен Модул & Фикс за Гости**: Интегриран ManageAds.jsx, AdBanner.jsx.
- [2026-05-15] **Динамичен Акцент**: Реализиран FeaturedRecipe в Home.jsx.
- [2026-05-15] **No-Index Strategy**: Преминаване към клиентско сортиране и филтриране за елиминиране на нуждите от композитни индекси в Home и AdBanner.
- [2026-05-15] **Firestore Security**: Оптимизирани правила за публичен достъп (read: if true) за рецепти и реклами.
- [2026-05-15] **Robust Import**: Подобрена логика за импорт с обработка на грешен JSON и премахнати имейл ограничения.
- [2026-05-15] **UX Cleanup**: Изтриването на акаунт е в "Basic Info", "Owner Section" е скрита с таен активатор.

## [2026-05-14] - Административна сигурност и фиксове в модул Рецепти
### Добавено (Added)
- **Засилена RBAC защита**:
  - Ограничен достъп до "Дневник на действията" (Activity Log) само за потребители с роля `OWNER`.
  - Защита на `OWNER` профилите: Администраторите вече не могат да деактивират или изтриват акаунти на собственици.
  - Динамично скриване на чувствителни административни бутони в списъка с потребители, ако целта е `OWNER`.
- **Реорганизация на Профила**:
  - Бутонът "Изход" е преместен на по-видно място под описанието на профила.
  - Нова секция "АДМИНИСТРАЦИЯ" (Administration) с отличителна двойна червена рамка и червени бутони за подчертаване на важността им.
  - Бутонът "Add Data" е преименуван на "Редактиране Рецепти/Продукти".

### Поправено (Fixed)
- **Модул Рецепти**:
  - Коригиран критичен бъг, при който полето за мерни единици оставаше неактивно (disabled) след избор на продукт.
  - Премахнати колизии на ID-та при динамичните редове (съставки и стъпки) чрез уникални идентификатори.
  - **Двуезични бележки**: Добавени отделни полета за бележки на български и английски език за всяка съставка.
  - **Нормализация на порциите**: Имплементирана логика за съхранение на съставките за точно 1 порция. Автоматично мащабиране в административния UI при промяна на броя порции.
  - **Валидация при запис**: Добавена задължителна проверка за попълнени количества и мерни единици за всички избрани продукти.
  - **UI Оптимизация**: Компактен изглед на съставките с фиксирани ширини и защита от преливане (overflow). Полето "Порции" е преместено в таб "Съставки".

## [2026-05-14] - Advanced Backup & Recovery (Cloud Integration)
### Добавено (Added)
- **Облачни Архиви (Cloud Backups)**:
  - Интеграция с **Firebase Storage** за съхранение на пълни JSON снимки на базата данни.
  - Автоматизирано управление на архивите през UI (Списък с дата/час и размер).
  - Система за автоматично почистване: Пазят се само последните 5 архива за пестене на място.
  - Функционалност за директно възстановяване от облака и изтриване на стари копия.
- **Разширен Локален Експорт**: Добавени са колекциите `system_history`, `user_pantry` и `ingredient_groups` към списъка за архивиране.
- **Сигурност**: Въведени са задължителни потвърждения (Confirm Dialogs) за всички деструктивни действия.

## [2026-05-13] - Възстановяване на административния достъп и RBAC фиксове
### Добавено (Added)
- **Системен Архив (Undo)**: Нов модул за преглед на исторически версии на рецепти, продукти и мерни единици с възможност за възстановяване с един клик.
- **Връщане на промени**: Интегриран механизъм за "Undo", который предпазва от злоумишлено изтриване или промяна на данни.

### Поправено (Fixed)
- **Административен достъп**: Възстановена функционалността на модулите "Потребители" и "Дневник на действията".
- **Синхронизация на ролите**: Коригирано несъответствието между Firestore (`Administrator`) и кода (`admin`) чрез мапинг в `AuthContext`.
- **Стабилност на интерфейса**: Премахнати критични ограничения на височината (`h-screen`), които причиняваха празни екрани.
- **ProtectedRoute**: Добавена е визуална сигнализация при грешка в достъпа и разширено логиране за диагностика.
- **Linting**: Изчистени всички 8 критични линт грешки за стабилен билд.

## [2026-05-10] - Оптимизация на изображения, Локализация и CSV Управление
### Добавено (Added)
- **Оптимизация на изображения**:
  - Въведен помощен модул `src/lib/imageUtils.js` за клиентско преоразмеряване на снимки.
  - Автоматично мащабиране до 800px при качване на профилни снимки и снимки на рецепти.
- **CSV Импорт/Експорт (Админ)**:
  - Пълна функционалност за експорт на продукти в CSV формат.
  - Интелигентен 2-стъпков импорт с модален прозорец за потвърждение.
  - Автоматично засичане на дубликати по slug и име (BG/EN).
- **Локализация на Профили**:
  - Добавени полета за град и държава на два езика (`city_bg/en`, `country_bg/en`).
  - Разделяне на "За мен" (Bio) на два езика за по-добро потребителско преживяване.
- **Класификация на Рецепти**:
  - Нова статична таксономия в `src/data/recipe_categories.js`.
  - 10 основни групи и 45 подкатегории с икони.
